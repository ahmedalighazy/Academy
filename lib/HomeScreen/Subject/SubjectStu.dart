import 'package:flutter/material.dart';

class SubjectStu extends StatelessWidget {
  static const String routeName = 'SubjectStu';
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pinkAccent,
    );
  }
}
