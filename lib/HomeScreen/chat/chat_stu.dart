import 'package:flutter/material.dart';

class ChatStu extends StatelessWidget {
  static const String routeName = 'chat';
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
